import { DirectorDashboardPage } from '@/modules/dashboard';

/** Mở dashboard giám đốc ở phân hệ phân tích xét nghiệm. */
export default function DirectorLabAnalyticsRoute() {
  return <DirectorDashboardPage section="lab" />;
}
