# HMS Backend

Express API using an expanded MVC structure.

HTTP routes belong in `src/routes` or each domain module's `routes`.
Controllers handle transport, services handle business rules, and repositories
own Prisma data access.
